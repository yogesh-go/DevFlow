PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/devflow
JWT_SECRET=your_jwt_secret_key_here

# Optional: External LLM provider for AI Developer Suite
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: GitHub token to increase rate limit for GitHub analytics
GITHUB_TOKEN=
